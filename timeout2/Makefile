# Generated by ./config.exe configure -t unix (2020-01-20 15:43:26-00:00).

-include Makefile.user

OPAM = opam
DEPEXT ?= $(OPAM) pin add -k path --no-action --yes mirage-unikernel-timeout2-unix . &&\
	$(OPAM) depext --yes --update mirage-unikernel-timeout2-unix ;\
	$(OPAM) pin remove --no-action mirage-unikernel-timeout2-unix

.PHONY: all depend depends clean build
all:: build

depend depends::
	$(DEPEXT)
	$(OPAM) install -y --deps-only .

build::
	mirage build

clean::
	mirage clean

