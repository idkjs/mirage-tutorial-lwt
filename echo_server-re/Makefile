# Generated by ./config.exe configure -t unix (2020-01-20 15:54:29-00:00).

-include Makefile.user

OPAM = opam
DEPEXT ?= $(OPAM) pin add -k path --no-action --yes mirage-unikernel-echo_server-unix . &&\
	$(OPAM) depext --yes --update mirage-unikernel-echo_server-unix ;\
	$(OPAM) pin remove --no-action mirage-unikernel-echo_server-unix

.PHONY: all depend depends clean build
all:: build

depend depends::
	$(DEPEXT)
	$(OPAM) install -y --deps-only .

build::
	mirage build

clean::
	mirage clean

